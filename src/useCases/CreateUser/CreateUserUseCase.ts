/* The actions that an specific user can perform into our app

This folder would contain the core functionalities that can 
be performed with the app. In this case we just have one function,
to create a new user

*/

export class CreateUserUseCase {
  async execute() {}
}
